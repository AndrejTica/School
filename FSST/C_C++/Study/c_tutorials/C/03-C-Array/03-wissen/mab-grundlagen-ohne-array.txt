mab: C-grundlagen
---------------------------------------------------------------

1. (3) Was ist an diesem Programm falsch?
---------------------------------------------------------------
Zeile1:	#include <stdio.h>
Zeile2:	int main(){
Zeile3:	  int alter, groesse, gewicht;

Zeile4:	  printf("Wie alt sind Sie :");
Zeile5:	  scanf("%i",alter);
Zeile6:	  printf("Wie gross sind Sie:);
Zeile7:	  scanf("%i",groesse)
Zeile8:	  printf("Wie schwer sind Sie:");
Zeile9:	  scanf("%i",gewicht);
Zeile10:  printf("Alter %i, Grösse: %i, Gewicht %i\n" ,alter,groesse,gewicht);


2. (3) in2out.c ergänzen
---------------------------------------------------------------
#include <stdio.h>

int main(){
    int ch;
    int ziffern=0;
    int gross=0;
    int klein=0;
    int alle=0;
    
    // zeichenweises einlesen und ausgeben
    ch= fgetc(stdin);
    while(ch != EOF){
a)     if(?????????????){
            klein++;
        }
b)     ????????? if(????????????????){
            gross++;
        }
        ????????? if(???????????????????){
            ziffern++;
        }   
        alle++;
        
        ch= fgetc(stdin);
    }
     
c) ??????("Alle=????, Ziffern=????, Gross=????, klein=????", ?????);
				
    return 0;
}

3. (3) in2out.c ergänzen
---------------------------------------------------------------
/* Datei: u_atoi.c   
 * Liest einen Ziffernstring ein und 
 * Wandelt diesen in eine INT-Zahl um und
 * Gibt die umgewandelte INT-Zahl aus
 */

#include <stdio.h>

int main(){
	int ch, num=???????;

	ch=fgetc(stdin); // oder   ch=getchar()
	while (ch != '\n'){
		num= ?????????;
		ch= fgetc(stdin);
	}

	printf("\n Die eingegebene Zahl war %d\n", ??????);

	return 0;
}

