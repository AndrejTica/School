/**
 *@brief hex-Ziffern in int Zahl umwandeln

 @file	hex2int.c
 @author N.N.
 @description
 Liest einen HEX-Ziffernstring ein und
 Wandelt diesen in eine INT-Zahl um und
 Gibt die umgewandelte INT-Zahl aus
 */

#include <stdio.h>
#include <stdlib.h>

int main(){
	int ch, num=0;

	ch=fgetc(stdin);
	while (ch != '\n'){

		????????????????????????


		ch=fgetc(stdin);
	}

	printf("\n Die eingegebene Zahl war %i\n", num);

	return 0;
}

